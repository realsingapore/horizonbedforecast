FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src ./src
COPY artifacts ./artifacts

ENV PYTHONPATH=/app

CMD ["uvicorn", "src.horizon_forecasting.api.main:app", "--host", "0.0.0.0", "--port", "8000"]